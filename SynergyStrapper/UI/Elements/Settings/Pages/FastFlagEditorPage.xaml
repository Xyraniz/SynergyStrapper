<ui:UiPage x:Class="SynergyStrapper.UI.Elements.Settings.Pages.FastFlagEditorPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:controls="clr-namespace:SynergyStrapper.UI.Elements.Controls"
      xmlns:resources="clr-namespace:SynergyStrapper.Resources"
      mc:Ignorable="d" 
      d:DesignHeight="650" d:DesignWidth="980"
      Title="FastFlagEditorPage"
      Loaded="Page_Loaded">
    <Grid Margin="0,0,14,14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Margin="0,0,0,2" Text="{x:Static resources:Strings.Menu_FastFlagEditor_Description}" FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
        <controls:MarkdownTextBlock TextWrapping="Wrap" Grid.Row="1" FontSize="14" FontWeight="Bold" Margin="0,0,0,12" MarkdownText="{Binding Source={x:Static resources:Strings.Menu_FastFlagEditor_AllowlistInformation}, Converter={StaticResource StringFormatConverter}, ConverterParameter='https://devforum.roblox.com/t/allowlist-for-local-client-configuration-via-fast-flags/3966569'}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

        <ui:InfoBar Grid.Row="2" Margin="0,0,0,16" Padding="8" Title="{x:Static resources:Strings.Menu_FastFlagEditor_BannerTitle}" IsOpen="True" Message="{x:Static resources:Strings.Menu_FastFlagEditor_BannerText}" Severity="Warning" IsClosable="False" />

        <Grid Grid.Row="3" Margin="0,0,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <WrapPanel Grid.Column="0" Orientation="Horizontal">
                <ui:Button Icon="ArrowLeft48" Content="{x:Static resources:Strings.Menu_FastFlagEditor_Back}" Click="BackButton_Click" Margin="0,0,12,16" />
                <ui:Button Icon="Add28" Content="{x:Static resources:Strings.Menu_FastFlagEditor_AddNew}" Click="AddButton_Click" Margin="0,0,12,16" />
                <ui:Button Icon="Delete48" Content="{x:Static resources:Strings.Menu_FastFlagEditor_DeleteSelected}" Click="DeleteButton_Click" Appearance="Danger" Margin="0,0,12,16">
                    <ui:Button.Style>
                        <Style TargetType="ui:Button" BasedOn="{StaticResource {x:Type ui:Button}}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ElementName=DataGrid, Path=SelectedItems.Count}" Value="0">
                                    <Setter Property="IsEnabled" Value="False" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ui:Button.Style>
                </ui:Button>
                <ui:Button Icon="Delete48" Content="Delete all" Click="DeleteAllButton_Click" Margin="0,0,12,16" />
                <ToggleButton x:Name="TogglePresetsButton" Content="{x:Static resources:Strings.Menu_FastFlagEditor_ShowPresetFlags}" Click="ToggleButton_Click" Margin="0,0,12,16" />
                <ui:Button Icon="ArrowExportRtl24" Content="{x:Static resources:Strings.Menu_FastFlagEditor_ExportJson}" Click="ExportJSONButton_Click" Margin="0,0,12,16" />
                <ui:Button Content="Save JSON" Click="SaveJSONButton_Click" Margin="0,0,12,16" />
                <ui:Button Content="Health check" Click="HealthCheckButton_Click" Margin="0,0,12,16" />
                <ComboBox x:Name="ProfileComboBox" Width="170" Margin="0,0,8,16" VerticalContentAlignment="Center" />
                <ui:Button Content="Save profile" Click="SaveProfileButton_Click" Margin="0,0,8,16" />
                <ui:Button Content="Load profile" Click="LoadProfileButton_Click" Margin="0,0,8,16" />
                <ui:Button Content="Delete profile" Click="DeleteProfileButton_Click" Appearance="Danger" Margin="0,0,0,16" />
            </WrapPanel>
            <StackPanel Grid.Column="1" Orientation="Vertical" HorizontalAlignment="Right" Margin="12,0,0,16">
                <TextBlock x:Name="TotalFlagsTextBlock" Text="Flags: 0" HorizontalAlignment="Right" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                <TextBlock x:Name="HistoryCountTextBlock" Text="Changes: 0" HorizontalAlignment="Right" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                <TextBlock x:Name="HealthStatusTextBlock" Text="Health: not checked" HorizontalAlignment="Right" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
            </StackPanel>
        </Grid>

        <ui:TextBox x:Name="SearchTextBox" Grid.Row="4" Margin="0,0,0,8" Icon="Search32" PlaceholderText="Search FastFlags" TextChanged="SearchTextBox_TextChanged" />

        <Border Grid.Row="5" CornerRadius="8" Background="Transparent" BorderBrush="{DynamicResource ControlElevationBorderBrush}" BorderThickness="1" SnapsToDevicePixels="True" ClipToBounds="True" >
            <DataGrid Name="DataGrid" HeadersVisibility="Column" GridLinesVisibility="None" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" CanUserReorderColumns="False" CellEditEnding="DataGrid_CellEditEnding" RowHeight="32" AlternatingRowBackground="{DynamicResource ControlFillColorTertiaryBrush}" Background="Transparent" BorderThickness="0">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="{DynamicResource ControlFillColorTertiaryBrush}" />
                        <Setter Property="Height" Value="32" />
                        <Setter Property="Padding" Value="10,0,10,0" />
                        <Setter Property="BorderBrush" Value="{DynamicResource ControlElevationBorderBrush}" />
                        <Setter Property="BorderThickness" Value="0,0,1,1" />
                        <Setter Property="HorizontalContentAlignment" Value="Left" />
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="8,0,8,0" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Foreground"><Setter.Value><SolidColorBrush Color="{DynamicResource TextOnAccentFillColorPrimary}" /></Setter.Value></Setter>
                                <Setter Property="Background"><Setter.Value><SolidColorBrush Color="{DynamicResource SystemAccentColorSecondary}" /></Setter.Value></Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Tags" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ItemsControl ItemsSource="{Binding Tags}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel VerticalAlignment="Center" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Margin="2,0" Padding="6,1" CornerRadius="6" Background="{DynamicResource ControlFillColorTertiaryBrush}">
                                                <TextBlock Text="{Binding}" FontSize="10" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Preset" Width="60">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ui:SymbolIcon Symbol="CheckmarkCircle32" Width="18" Height="18" HorizontalAlignment="Center">
                                    <ui:SymbolIcon.Style>
                                        <Style TargetType="ui:SymbolIcon" BasedOn="{StaticResource {x:Type ui:SymbolIcon}}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding HasPreset}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ui:SymbolIcon.Style>
                                </ui:SymbolIcon>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="{x:Static resources:Strings.Common_Name}" Binding="{Binding Name, UpdateSourceTrigger=LostFocus}" Width="2*" />
                    <DataGridTextColumn Header="{x:Static resources:Strings.Common_Value}" Binding="{Binding Value, UpdateSourceTrigger=LostFocus}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Expander Grid.Row="6" Margin="0,8,0,0" Background="{DynamicResource ControlFillColorTertiaryBrush}" Header="Flag history" IsExpanded="False">
            <ScrollViewer CanContentScroll="True" VerticalScrollBarVisibility="Auto">
                <ListBox x:Name="HistoryListBox" MaxHeight="180" Background="Transparent" BorderThickness="0" FontSize="12" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
            </ScrollViewer>
        </Expander>
    </Grid>
</ui:UiPage>
