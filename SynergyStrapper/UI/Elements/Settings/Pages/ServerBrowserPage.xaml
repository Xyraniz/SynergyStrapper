<ui:UiPage x:Class="SynergyStrapper.UI.Elements.Settings.Pages.ServerBrowserPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d"
      Title="Server Browser"
      Loaded="Page_Loaded"
      Unloaded="Page_Unloaded">
    <Grid Margin="0,0,14,14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontSize="20" FontWeight="SemiBold" Text="Server browser" />
        <TextBlock Grid.Row="1" Margin="0,4,0,12" FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" Text="Find public Roblox servers by Place ID, compare player counts and choose a server with the lowest reported ping." />

        <Grid Grid.Row="1" Margin="0,54,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ui:TextBox x:Name="PlaceIdTextBox" Grid.Column="0" Margin="0,0,8,0" PlaceholderText="Place ID" TextChanged="PlaceIdTextBox_TextChanged" />
            <ComboBox x:Name="SortComboBox" Grid.Column="1" Width="150" Margin="0,0,8,0" SelectionChanged="SortComboBox_SelectionChanged">
                <ComboBoxItem Content="Lowest ping" Tag="ping" IsSelected="True" />
                <ComboBoxItem Content="Fewest players" Tag="players" />
                <ComboBoxItem Content="Most players" Tag="mostPlayers" />
            </ComboBox>
            <ComboBox x:Name="LimitComboBox" Grid.Column="2" Width="80" Margin="0,0,8,0" SelectionChanged="LimitComboBox_SelectionChanged">
                <ComboBoxItem Content="25" Tag="25" />
                <ComboBoxItem Content="50" Tag="50" IsSelected="True" />
                <ComboBoxItem Content="100" Tag="100" />
            </ComboBox>
            <ui:Button Grid.Column="3" Content="Refresh" Icon="ArrowClockwise24" Click="RefreshButton_Click" />
        </Grid>

        <DataGrid x:Name="ServersGrid" Grid.Row="2" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" SelectionMode="Single" MouseDoubleClick="ServersGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Players" Binding="{Binding PlayerSummary}" Width="140" />
                <DataGridTextColumn Header="Ping" Binding="{Binding PingSummary}" Width="110" />
                <DataGridTextColumn Header="FPS" Binding="{Binding FpsSummary}" Width="90" />
                <DataGridTextColumn Header="Server ID" Binding="{Binding Id}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="3" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="StatusTextBlock" Grid.Column="0" VerticalAlignment="Center" Text="Enter a Place ID to load public servers." TextWrapping="Wrap" />
            <ui:Button Grid.Column="1" Margin="8,0,8,0" Content="Copy server ID" Click="CopyButton_Click" />
            <ui:Button Grid.Column="2" Content="Join selected" Appearance="Primary" Click="JoinButton_Click" />
        </Grid>
    </Grid>
</ui:UiPage>
