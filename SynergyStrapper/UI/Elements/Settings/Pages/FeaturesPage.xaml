<ui:UiPage x:Class="SynergyStrapper.UI.Elements.Settings.Pages.FeaturesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:dmodels="clr-namespace:SynergyStrapper.UI.ViewModels.Settings"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance dmodels:FeaturesViewModel, IsDesignTimeCreatable=True}"
      Title="FeaturesPage"
      Scrollable="True">
    <StackPanel Margin="0,0,14,14">
        <TextBlock Text="SynergyStrapper 1.0.8 features" FontSize="22" FontWeight="SemiBold" />
        <TextBlock Text="All advanced features are opt-in unless explicitly noted. Save settings after changing options." Margin="0,4,0,16" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />

        <GroupBox Header="Deployment and lifecycle" Padding="10">
            <StackPanel>
                <CheckBox Content="Allow controlled multi-instance launches" IsChecked="{Binding AllowMultipleInstances, Mode=TwoWay}" />
                <TextBlock Text="Some experiences or policies may not accept multiple Roblox clients." Margin="24,0,0,8" FontSize="11" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                <CheckBox Content="Keep the multi-instance watcher alive" IsChecked="{Binding KeepMultiInstanceWatcherAlive, Mode=TwoWay}" />
                <CheckBox Content="Clean residual channel pins when production is selected" IsChecked="{Binding EnableChannelPinGuard, Mode=TwoWay}" Margin="0,8,0,0" />
                <CheckBox Content="Use an optional static Roblox installation directory" IsChecked="{Binding UseStaticDirectory, Mode=TwoWay}" Margin="0,8,0,0" />
                <DockPanel Margin="24,4,0,0">
                    <Button DockPanel.Dock="Right" Content="Browse" Command="{Binding BrowseStaticDirectoryCommand}" />
                    <TextBox Text="{Binding StaticDirectory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0" />
                </DockPanel>
                <TextBlock Text="The static directory is restricted to a safe user-data path and receives a version manifest." Margin="24,2,0,8" FontSize="11" Foreground="{DynamicResource TextFillColorTertiaryBrush}" TextWrapping="Wrap" />
                <CheckBox Content="Minimize settings to tray" IsChecked="{Binding MinimizeToTray, Mode=TwoWay}" />
                <CheckBox Content="Keep watcher and integrations running in tray" IsChecked="{Binding KeepRunningInTray, Mode=TwoWay}" />
                <CheckBox Content="Close settings after launching Roblox" IsChecked="{Binding CloseSettingsOnLaunch, Mode=TwoWay}" />
            </StackPanel>
        </GroupBox>

        <GroupBox Header="Configurable cleaner" Padding="10" Margin="0,12,0,0">
            <StackPanel>
                <WrapPanel>
                    <CheckBox Content="Synergy logs" IsChecked="{Binding CleanSynergyLogs, Mode=TwoWay}" Margin="0,0,14,0" />
                    <CheckBox Content="Synergy downloads" IsChecked="{Binding CleanSynergyDownloads, Mode=TwoWay}" Margin="0,0,14,0" />
                    <CheckBox Content="Roblox logs" IsChecked="{Binding CleanRobloxLogs, Mode=TwoWay}" Margin="0,0,14,0" />
                    <CheckBox Content="Roblox cache" IsChecked="{Binding CleanRobloxCache, Mode=TwoWay}" />
                </WrapPanel>
                <WrapPanel Margin="0,8,0,0">
                    <TextBlock Text="Max age (days):" VerticalAlignment="Center" />
                    <TextBox Width="80" Text="{Binding CleanerMaxAgeDays, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="8,0,18,0" />
                    <TextBlock Text="Max files per directory:" VerticalAlignment="Center" />
                    <TextBox Width="80" Text="{Binding CleanerMaxFiles, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="8,0,0,0" />
                </WrapPanel>
                <WrapPanel Margin="0,10,0,0">
                    <Button Content="Preview" Command="{Binding PreviewCleanerCommand}" Margin="0,0,8,0" />
                    <Button Content="Clean now" Command="{Binding CleanNowCommand}" />
                </WrapPanel>
                <TextBlock Text="{Binding StatusText}" Margin="0,10,0,0" TextWrapping="Wrap" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            </StackPanel>
        </GroupBox>

        <GroupBox Header="Runtime maintenance" Padding="10" Margin="0,12,0,0">
            <StackPanel>
                <CheckBox Content="Enable optional memory trimmer" IsChecked="{Binding EnableMemoryTrimmer, Mode=TwoWay}" />
                <WrapPanel Margin="24,6,0,0">
                    <TextBlock Text="Threshold (MB):" VerticalAlignment="Center" />
                    <TextBox Width="80" Text="{Binding MemoryTrimThresholdMb, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="8,0,18,0" />
                    <TextBlock Text="Interval (seconds):" VerticalAlignment="Center" />
                    <TextBox Width="80" Text="{Binding MemoryTrimIntervalSeconds, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="8,0,0,0" />
                </WrapPanel>
                <CheckBox Content="Close RobloxCrashHandler.exe during a session" IsChecked="{Binding DisableCrashHandler, Mode=TwoWay}" Margin="0,10,0,0" />
                <TextBlock Text="Closing the crash handler may reduce diagnostics and is disabled by default." Margin="24,2,0,0" FontSize="11" Foreground="{DynamicResource TextFillColorTertiaryBrush}" TextWrapping="Wrap" />
            </StackPanel>
        </GroupBox>

        <GroupBox Header="Roblox appStorage.json" Padding="10" Margin="0,12,0,0">
            <StackPanel>
                <CheckBox Content="Enable managed appStorage changes" IsChecked="{Binding AppStorageEnabled, Mode=TwoWay}" />
                <WrapPanel Margin="24,8,0,0">
                    <CheckBox Content="Dark theme" IsChecked="{Binding AppStorageDarkTheme, Mode=TwoWay}" Margin="0,0,14,0" />
                    <CheckBox Content="Minimize to tray" IsChecked="{Binding AppStorageMinimizeToTray, Mode=TwoWay}" Margin="0,0,14,0" />
                    <CheckBox Content="Launch at startup" IsChecked="{Binding AppStorageLaunchAtStartup, Mode=TwoWay}" />
                </WrapPanel>
                <WrapPanel Margin="24,8,0,0">
                    <CheckBox Content="Hide version details" IsChecked="{Binding AppStorageHideVersion, Mode=TwoWay}" Margin="0,0,14,0" />
                    <CheckBox Content="Hide production details" IsChecked="{Binding AppStorageHideProduction, Mode=TwoWay}" />
                </WrapPanel>
                <Button Content="Apply with backup" Command="{Binding ApplyAppStorageCommand}" Margin="0,10,0,0" HorizontalAlignment="Left" />
            </StackPanel>
        </GroupBox>

        <GroupBox Header="Audio, cursors and icons" Padding="10" Margin="0,12,0,0">
            <StackPanel>
                <WrapPanel>
                    <Button Content="Choose death sound" Command="{Binding ChooseDeathSoundCommand}" Margin="0,0,8,0" />
                    <Button Content="Remove death sound" Command="{Binding RemoveDeathSoundCommand}" />
                </WrapPanel>
                <TextBlock Text="Cursors are independent and stored in Synergy's managed modifications folder." Margin="0,10,0,4" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                <UniformGrid Columns="2">
                    <StackPanel Margin="0,4,8,4">
                        <TextBlock Text="Arrow" FontWeight="SemiBold" />
                        <WrapPanel><Button Content="Choose" Command="{Binding ChooseCursorCommand}" CommandParameter="Arrow" Margin="0,4,6,0" /><Button Content="Remove" Command="{Binding RemoveCursorCommand}" CommandParameter="Arrow" Margin="0,4,0,0" /></WrapPanel>
                    </StackPanel>
                    <StackPanel Margin="8,4,0,4">
                        <TextBlock Text="ArrowFar" FontWeight="SemiBold" />
                        <WrapPanel><Button Content="Choose" Command="{Binding ChooseCursorCommand}" CommandParameter="ArrowFar" Margin="0,4,6,0" /><Button Content="Remove" Command="{Binding RemoveCursorCommand}" CommandParameter="ArrowFar" Margin="0,4,0,0" /></WrapPanel>
                    </StackPanel>
                    <StackPanel Margin="0,4,8,4">
                        <TextBlock Text="IBeam" FontWeight="SemiBold" />
                        <WrapPanel><Button Content="Choose" Command="{Binding ChooseCursorCommand}" CommandParameter="IBeam" Margin="0,4,6,0" /><Button Content="Remove" Command="{Binding RemoveCursorCommand}" CommandParameter="IBeam" Margin="0,4,0,0" /></WrapPanel>
                    </StackPanel>
                    <StackPanel Margin="8,4,0,4">
                        <TextBlock Text="Shiftlock" FontWeight="SemiBold" />
                        <WrapPanel><Button Content="Choose" Command="{Binding ChooseCursorCommand}" CommandParameter="Shiftlock" Margin="0,4,6,0" /><Button Content="Remove" Command="{Binding RemoveCursorCommand}" CommandParameter="Shiftlock" Margin="0,4,0,0" /></WrapPanel>
                    </StackPanel>
                </UniformGrid>
                <WrapPanel Margin="0,8,0,0"><Button Content="Import cursor set" Command="{Binding ImportCursorSetCommand}" Margin="0,0,8,0" /><Button Content="Export cursor set" Command="{Binding ExportCursorSetCommand}" /></WrapPanel>
                <WrapPanel Margin="0,12,0,0"><Button Content="Choose Player icon" Command="{Binding ChooseIconCommand}" CommandParameter="Player" Margin="0,0,8,0" /><Button Content="Choose Studio icon" Command="{Binding ChooseIconCommand}" CommandParameter="Studio" /></WrapPanel>
            </StackPanel>
        </GroupBox>

        <GroupBox Header="FastFlags and performance" Padding="10" Margin="0,12,0,0">
            <StackPanel>
                <CheckBox Content="Check FastFlag availability against a versioned allowlist" IsChecked="{Binding EnableFastFlagAvailabilityCheck, Mode=TwoWay}" />
                <TextBox Text="{Binding FastFlagAllowlistUrl, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" ToolTip="Optional allowlist URL; cached data is retained when unavailable." Margin="0,8,0,0" />
                <Button Content="Refresh availability" Command="{Binding RefreshFastFlagAvailabilityCommand}" Margin="0,8,0,0" HorizontalAlignment="Left" />
                <WrapPanel Margin="0,12,0,0">
                    <TextBlock Text="Performance profile:" VerticalAlignment="Center" />
                    <ComboBox Width="150" ItemsSource="{Binding PerformanceProfiles}" SelectedItem="{Binding SelectedPerformanceProfile, Mode=TwoWay}" Margin="8,0,18,0" />
                    <TextBlock Text="FPS limit:" VerticalAlignment="Center" />
                    <ComboBox Width="100" ItemsSource="{Binding FpsOptions}" SelectedItem="{Binding FpsLimit, Mode=TwoWay}" Margin="8,0,0,0" />
                </WrapPanel>
                <TextBlock Text="FPS limits are requests: Roblox, the renderer, experience and hardware may ignore or cap them." Margin="0,4,0,0" FontSize="11" Foreground="{DynamicResource TextFillColorTertiaryBrush}" TextWrapping="Wrap" />
                <WrapPanel Margin="0,10,0,0"><Button Content="Apply profile" Command="{Binding ApplyPerformanceCommand}" Margin="0,0,8,0" /><Button Content="Rollback last profile" Command="{Binding RollbackPerformanceCommand}" /></WrapPanel>
            </StackPanel>
        </GroupBox>

        <GroupBox Header="Studio-first mod packs" Padding="10" Margin="0,12,0,0">
            <StackPanel>
                <TextBlock Text="Create a pack folder, place files in ModFiles, and prefix a file with DELETE  to remove a resource with a backup." TextWrapping="Wrap" />
                <WrapPanel Margin="0,8,0,0"><TextBlock Text="Pack name:" VerticalAlignment="Center" /><TextBox Width="180" Text="{Binding StudioPackName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="8,0,8,0" /><Button Content="Open pack folder" Command="{Binding OpenStudioPackFolderCommand}" Margin="0,0,8,0" /><Button Content="Apply to Studio" Command="{Binding ApplyStudioPackCommand}" /></WrapPanel>
                <CheckBox Content="Keep DELETE operations reversible and require explicit user review" IsChecked="{Binding ConfirmStudioDeletes, Mode=TwoWay}" Margin="0,8,0,0" />
            </StackPanel>
        </GroupBox>

        <GroupBox Header="In-game overlay and history" Padding="10" Margin="0,12,0,0">
            <StackPanel>
                <CheckBox Content="Enable click-through informational overlay" IsChecked="{Binding EnableGameOverlay, Mode=TwoWay}" />
                <WrapPanel Margin="24,8,0,0"><CheckBox Content="Clock" IsChecked="{Binding OverlayShowClock, Mode=TwoWay}" Margin="0,0,14,0" /><CheckBox Content="Region" IsChecked="{Binding OverlayShowRegion, Mode=TwoWay}" Margin="0,0,14,0" /><CheckBox Content="Ping" IsChecked="{Binding OverlayShowPing, Mode=TwoWay}" Margin="0,0,14,0" /><CheckBox Content="Dimmer" IsChecked="{Binding OverlayDimmerEnabled, Mode=TwoWay}" /></WrapPanel>
                <TextBlock Text="The overlay does not inject into or modify Roblox and is disabled by default." Margin="24,2,0,8" FontSize="11" Foreground="{DynamicResource TextFillColorTertiaryBrush}" TextWrapping="Wrap" />
                <CheckBox Content="Show region in Recent Games" IsChecked="{Binding ShowHistoryRegion, Mode=TwoWay}" />
                <CheckBox Content="Show total play time in Recent Games" IsChecked="{Binding ShowHistoryTotalTime, Mode=TwoWay}" />
            </StackPanel>
        </GroupBox>
    </StackPanel>
</ui:UiPage>
