<ui:UiPage x:Class="SynergyStrapper.UI.Elements.Settings.Pages.HistoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d"
      Title="Recent Games"
      Loaded="Page_Loaded">
    <Grid Margin="0,0,14,14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" FontSize="20" FontWeight="SemiBold" Text="Recent games" />
        <TextBlock Grid.Row="1" Margin="0,4,0,12" FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="Your last 50 locally recorded game sessions. No cookies, tokens or account credentials are stored." TextWrapping="Wrap" />
        <DataGrid Grid.Row="2" x:Name="HistoryGrid" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" SelectionMode="Single" MouseDoubleClick="HistoryGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Game" Binding="{Binding GameName}" Width="*" />
                <DataGridTextColumn Header="Place ID" Binding="{Binding PlaceId}" Width="120" />
                <DataGridTextColumn Header="Last played" Binding="{Binding LastPlayedSummary}" Width="180" />
                <DataGridTextColumn Header="Server ID" Binding="{Binding JobId}" Width="220" />
            </DataGrid.Columns>
        </DataGrid>
        <Grid Grid.Row="3" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="StatusTextBlock" Grid.Column="0" VerticalAlignment="Center" Text="" />
            <ui:Button Grid.Column="1" Margin="8,0,8,0" Content="Join selected" Appearance="Primary" Click="JoinButton_Click" />
            <ui:Button Grid.Column="2" Content="Clear history" Appearance="Danger" Click="ClearButton_Click" />
        </Grid>
    </Grid>
</ui:UiPage>
