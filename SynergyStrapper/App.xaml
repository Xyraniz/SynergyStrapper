<Application x:Class="SynergyStrapper.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:SynergyStrapper"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:converters="clr-namespace:SynergyStrapper.UI.Converters"
             ShutdownMode="OnExplicitShutdown"
             DispatcherUnhandledException="GlobalExceptionHandler">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ThemesDictionary Theme="Dark" />
                <ui:ControlsDictionary />
                <ResourceDictionary x:Name="CustomTheme" Source="UI/Style/Dark.xaml" /> <!-- NOTE: WpfUiWindow::ApplyTheme relies on this order. If you plan to change the order, please update the index in the function. -->
                <ResourceDictionary x:Name="Default" Source="UI/Style/Default.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Synergy Strapper monochrome accent palette. -->
            <Color x:Key="SystemAccentColor">#B8B8B8</Color>
            <Color x:Key="SystemAccentColorSecondary">#8A8A8A</Color>
            <Color x:Key="SystemAccentColorTertiary">#D8D8D8</Color>
            <SolidColorBrush x:Key="SystemAccentColorBrush" Color="{DynamicResource SystemAccentColor}" />
            <SolidColorBrush x:Key="SystemAccentColorSecondaryBrush" Color="{DynamicResource SystemAccentColorSecondary}" />
            <SolidColorBrush x:Key="SystemAccentColorTertiaryBrush" Color="{DynamicResource SystemAccentColorTertiary}" />

            <FontFamily x:Key="Rubik">pack://application:,,,/Resources/Fonts/#Rubik Light</FontFamily>

            <Style TargetType="Hyperlink">
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Foreground">
                            <Setter.Value>
                                <SolidColorBrush Color="{DynamicResource SystemAccentColorTertiary}" />
                            </Setter.Value>
                        </Setter>
                        <Setter Property="TextDecorations" Value="Underline" />
                    </Trigger>
                </Style.Triggers>
                <Setter Property="TextDecorations" Value="None" />
                <Setter Property="Foreground">
                    <Setter.Value>
                        <SolidColorBrush Color="{DynamicResource SystemAccentColorSecondary}" />
                    </Setter.Value>
                </Setter>
            </Style>

            <converters:StringFormatConverter x:Key="StringFormatConverter" />
            <converters:RangeConverter x:Key="RangeConverter" />
            <converters:EnumNameConverter x:Key="EnumNameConverter" />
        </ResourceDictionary>
    </Application.Resources>
</Application>
